// variables6.rs
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a hint.

const NUMBER: i64 = 3;

fn main() {
    let add = NUMBER + 3;
    println!("Number {} add value: {}", NUMBER, add);
}
